--- a/one.models.jg/src/misc/ConnectionEstablishment/protocols/ConnectToInstance.ts
+++ b/one.models.jg/src/misc/ConnectionEstablishment/protocols/ConnectToInstance.ts
@@ -42,8 +42,11 @@ export async function connectToInstance(
         myPersonId = await leuteModel.myMainIdentity();
     }
 
-    const cryptoApi = await createCryptoApiFromDefaultKeys(myPersonId);
     const myInstanceId = await getLocalInstanceOfPerson(myPersonId);
+    
+    // CRITICAL FIX: Use instance keys for pairing, not person keys!
+    // The invitation contains the remote instance's public key, so we must use our instance keys
+    const cryptoApi = await createCryptoApiFromDefaultKeys(myInstanceId);
 
     // #### Encrypted connection handshake ####
     // Other side is implemented in IncomingConnectionManager.acceptConnection